Generate random number of elevators as N - Ready
Start thread pool of size N - Ready

Elevator sleeps
Elevator check weight limit being checked

PersonGenerator to generate a random person at random times - Done

Person has random values for destination, arrival and weight - Done
Person run: notify thread pool and sleep
Person woken: add to elevator and sleep
Person arrives: exit
Person: only one person per floor

Phil:
    familiarise the logic elevator and person generation
    work on movement of elevator
    elevator should move people from personqueue when lock is successful & add to elevator list
